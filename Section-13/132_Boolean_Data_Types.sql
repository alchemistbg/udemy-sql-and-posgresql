-- The following values are casted to TRUE
SELECT ('true'::BOOLEAN);

SELECT ('t'::BOOLEAN);

SELECT ('y'::BOOLEAN);

SELECT ('yes'::BOOLEAN);

SELECT ('on'::BOOLEAN);

SELECT (1::BOOLEAN);

SELECT (42::BOOLEAN);

-- The following values are casted to FALSE
SELECT ('false'::BOOLEAN);

SELECT ('f'::BOOLEAN);

SELECT ('n'::BOOLEAN);

SELECT ('no'::BOOLEAN);

SELECT ('off'::BOOLEAN);

SELECT (0::BOOLEAN);

SELECT (null::NULL);